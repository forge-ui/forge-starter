# GitHub Personal Access Token with read:packages scope.
# Get one at: https://github.com/settings/tokens
# Paste here, then `source .env` (or use direnv) before `pnpm install`.
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxx
